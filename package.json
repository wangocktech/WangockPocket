{
  "name": "wangock-pocket-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A pocket-console portfolio for Wangock. No build step or runtime dependencies.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check app.js && node --check content.js && node --check navigation.js && node --check sound.js && node --check games.js"
  }
}
